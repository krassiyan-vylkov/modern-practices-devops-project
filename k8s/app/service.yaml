apiVersion: v1
kind: Service
metadata:
  name: arknights-service
spec:
  type: NodePort
  selector:
    app: arknights
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 32369
